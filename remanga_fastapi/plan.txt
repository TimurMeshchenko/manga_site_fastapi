Пользователь, регистрация, вход, выход на jwt

защита от CSRF для post

При post, сравниваю headers с cookie, если разные raise ошибку и 
перезагружаю страницу и csrf_token добавляю в cookie и headers

объеденить 3 строчки generate_csrf_token в метод и вызывать его 
для всех get у который есть current_user (без middleware)

Для всех post utils.validate_csrf(request),(с middleware) если ошибка 
invalid_csrf_token перезагрузить страницу, чтобы получить новый,
если возможно то при вызове ошибки перенаправлять на этот же url, обновить,
а не в fetch по содержанию

Если выход, то удалить куки

После регистрации, входить

Могут ли перехватить fetch запрос, уязвимости post запросов

-------------------------------------------

Для тайтлов post
WebSokets вместо ajax

-------------------------------------------

Поиск, смена авы, пароля, infinity scroll

-------------------------------------------

Пароль у акков fsafsafzcx
python -m uvicorn main:app --reload

redis, RabbitMQ, asyncio, закинуть в docker

везде подсказки, что возвращает (в router response_model)
тесты, gitflow

переписать под mongodb

django docker через nginx + uvicorn, fastapi если получится

QA Automation Selenium, Bash, Web-UI, API

Если получится то создать для докера bash скрипты, для django,
как минимум переименование db host