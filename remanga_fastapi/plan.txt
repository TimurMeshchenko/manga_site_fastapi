johndoe secret

Пользователь, регистрация, вход, выход на jwt

Брать Пользователя с auth_user таблицы
Сделать поля в models, schemas как в таблице

Переписать utils.get_user, чтобы брал пользователя с бд 
и для убрать в аргументах бд для всех кто вызывает utils.get_user


Привязать к html
Спрос у ai регистрация fastapi

Для регистрации, отправлять через post данные из полей 
и создавать по ним пользователя (возможно есть готовая форма, как с входом)
В utils.create_user

Для авторизации также только с токеном
Спросить как сделать template с OAuth2PasswordRequestForm

Спросить про выход, думаю user.disabled = true

-------------------------------------------

Для тайтлов post
WebSokets вместо ajax

Пернести все запросы без template get,post в /api/
router = APIRouter(prefix="/api/")

-------------------------------------------

Поиск, смена авы, пароля, infinity scroll

-------------------------------------------

python -m uvicorn main:app --reload
redis + restapi, asyncio, закинуть в docker
Пароль у акков fsafsafzcx
